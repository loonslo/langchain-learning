# Day28–39（阶段3 Agent）第一性分析：能不能上 LangGraph 生产？

> 结论先行：**这一段是"能跑通、逻辑对、面试能讲"的合格教学线，但离"直接搬去生产"还差一层"可靠性 + 可观测性 + 落盘"。**
> 缺的不是概念，是把每个概念补上"出错了怎么办、出错了怎么查、重启了还在不在"这三问。
> 已用 4 个空号（29/31/32/34）把最关键的三问补齐，循序渐进插在对应位置。

---

## 一、第一性拆解：LangGraph 上生产到底要满足什么？

抛开框架名词，一个 Agent 要在真实业务里跑，本质要回答 6 个问题：

| # | 第一性问题 | 生产必需能力 | 当前 day28–39 覆盖 |
|---|-----------|-------------|-------------------|
| 1 | 状态怎么存、并发写怎么合？ | State schema + reducer（`Annotated` / `add_messages` / 自定义合并） | ⚠️ 只隐式用了 `MessagesState`，没讲 reducer 原理；fan-out 并发写 `angles` 靠手拼 `state[...]+[...]`，生产会丢更新 |
| 2 | 某一步出错了怎么办？ | 节点重试 / fallback / 工具报错不崩 / 超时 | ❌ 完全没有。所有节点都假设"一定成功" |
| 3 | 该走哪条分支谁说了算？ | 可靠路由（结构化输出，而非字符串 `in`） | ⚠️ Day39 用关键词 `"RESEARCH" in text` 路由，自己也批注"生产里换 LLM 决定"，但没实现 |
| 4 | 出了问题怎么定位？ | 可观测：分步流式 + 追踪(LangSmith) + 轨迹落盘 | ⚠️ Day36 只示范了 `stream_mode="updates"`；无 trace、无结构化轨迹落盘 |
| 5 | 重启 / 挂了还记得吗？ | 持久化落盘（Sqlite/Postgres saver）+ 长期记忆 | ⚠️ 全程 `InMemorySaver`（重启即丢）。Day35 口头提了落盘，实现推迟到 Day44 |
| 6 | 高风险动作谁拍板？ | HITL 审批 + 工具最小权限 | ✅ **这块做得好**。Day36/37 的 `interrupt` + "危险工具只模拟"是上线红线，讲透了 |

一句话：**第 6 问（安全）满分，第 2/3 问（可靠性/路由）是硬缺口，第 1/4/5 问（状态/可观测/落盘）是半成品。**

---

## 二、逐日体检（day28–39）

**Day28 基础（State/Node/Edge + 分支循环 + recursion_limit）**
概念干净，`recursion_limit` 防死循环是对的生产直觉。缺 reducer——学员会以为"节点返回啥就覆盖啥"，一到并发/累加就踩坑。→ 由 **Day29** 补。

**Day30 ReAct（手搭 + create_react_agent）**
手搭再给现成版，对比清楚，很好。但工具 `add/get_weather` 都不会失败，学员没见过"工具抛异常整张图崩掉"。→ 由 **Day31** 补。

**Day33 Plan-and-Execute + 范式速查**
范式速查表（ReAct/Reflexion/ReWOO/ToT）+ 框架对比，面试价值高，保留。属于"知道即可"，不用改。

**Day35 checkpoint + 上下文压缩**
`thread_id` 隔离、多轮记忆、摘要压缩讲得对。两个生产落差：(1) 只有 `InMemorySaver`，没演示落盘 saver；(2) 只有"同一会话内"记忆，没有"跨会话长期记忆"(`Store`)。落盘在 Day44 补，长期记忆建议后续加（见第四节）。

**Day36 streaming + HITL** ✅ 生产味最足的一天
`interrupt` 暂停 + `Command(resume=)` 恢复 + 危险工具只模拟，这套"AI 起草、人确认"是真实上线做法。唯一小遗憾：`stream_mode` 只示范了 `updates`，没讲 `values/messages/custom` 的区别。→ 由 **Day34** 补全。

**Day37 搜索+总结 Agent 端到端** ✅
把 ReAct + HITL + 持久化 + 轨迹日志串成作品，很好。轨迹日志是"护城河"加分项，但目前是 `print`，没结构化落盘（出错难复盘）。→ 由 **Day34** 升级成可复盘的结构化轨迹。

**Day38 Text2SQL（安全边界）** ✅ 亮点
`validate_sql` 四道守卫（只 SELECT / 白名单表 / 危险词拦截 / 强制 LIMIT）是标准的生产防线，且"哪怕换 LLM 生成也要保留这道门"的判断完全正确。可加：参数化查询防注入、SQL 执行超时。属锦上添花，不阻塞。

**Day39 Supervisor 多 Agent + Fan-out**
编排骨架对，`recursion_limit` 护栏在。两个生产落差：(1) 关键词路由脆弱（模型多说一个词就路由错）；(2) Fan-out 的 `angles` 用 `state["angles"] + [...]` 手拼，**并发分支会互相覆盖**——这正是没有 reducer 的直接后果。→ 路由由 **Day32** 补，并发合并由 **Day29** 补。

---

## 三、补全方案：4 个空号 → 4 个生产缺口（循序渐进）

不复刻被合并的旧 29/31/32/34，而是把最关键的生产缺口按"初学者能顺着往下走"的顺序填进空位：

| 空号 | 新主题 | 补的第一性问题 | 为什么放这里（进度衔接） |
|------|--------|--------------|----------------------|
| **Day29** | State 设计与 reducer | #1 状态/并发合并 | 紧接 28 基础：先懂"节点返回怎么合进 state"，才敢写并发和多 Agent。也直接修 Day39 fan-out 的并发覆盖 bug |
| **Day31** | 节点容错与重试 | #2 出错怎么办 | 紧接 30 ReAct：有了能跑的 Agent，立刻学"工具/模型报错不让整图崩"——生产第一课 |
| **Day32** | 结构化输出路由 | #3 分支谁拍板 | 接 31、垫 33/39：把 Day39 埋的"关键词路由太脆"坑，用 `with_structured_output` 正式解决 |
| **Day34** | 可观测性与调试 | #4 出错怎么查 | 接 33、垫 35：上持久化前先学会"看得见每一步"——4 种 stream_mode + LangSmith trace + 结构化轨迹落盘（护城河） |

补完后 28→34 的学习曲线：
`搭图(28) → 状态怎么合(29) → ReAct(30) → 出错兜底(31) → 可靠路由(32) → 规划范式(33) → 看得见可复盘(34) → 落盘记忆(35) → 流式+人审(36) → 端到端作品(37) → Text2SQL(38) → 多Agent(39)`
每一步只加一个新概念，符合初学者节奏。

---

## 四、仍建议后续补（不占本次 4 个空号，列入 backlog）

1. **持久化落盘（SqliteSaver/PostgresSaver）** — Day35 口头提了，Day44 才实现。可接受，但建议在 Day35 末尾加 3 行"一句话换落盘"的示例，避免学员一路只会 InMemory。
2. **长期记忆 `Store`（跨会话）** — checkpointer 只管"一个会话内"。企业知识库 Agent 要"记住这个用户上次说过什么"，需要 `langgraph.store`。建议作为 Day35 的扩展或独立小节。
3. **子图 / 组合（subgraph）** — 大项目把 Agent 拆成可复用子图。capstone 阶段用得上，现在不急。
4. **Day38 加固** — 参数化查询 + 执行超时。

---

## 五、给招聘官的一句话（这段学完能讲什么）

> "我的 Agent 不只是能跑：状态用 reducer 管并发合并；每个节点有重试和 fallback，工具报错不会让整图崩；分支路由用结构化输出而不是字符串匹配，稳定可测；高风险动作走 human-in-the-loop 审批、危险工具只给模拟权限；每一步都能流式观测、轨迹结构化落盘可复盘——这套'可靠 + 可观测 + 可审批'正是我测试背景的护城河。"

*(报告基于对 day28/30/33/35/36/37/38/39 源码的逐行审阅。空号 29/31/32/34 对应的新代码见同目录 day29/31/32/34 文件。)*
