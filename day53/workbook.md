# Day53 工作簿 · 真实产品回归评测

## 0. 先看本日变更闭环

| 文件 | 相对 Day52 | 关键问题 |
|---|---|---|
| `settings.py` | 修改 | `evaluation_path` 如何进入评测入口？ |
| `evaluation.py` | 新增 | 为什么必须调用正式 `assistant.ask()`？ |
| `data/eval_cases.json` | 新增 | 它固定了哪些可判定结果？ |
| `knowledge.py` / `ingestion.py` | 继承未改 | 评测为什么仍能走多文档主链？ |

先运行 `python tools/day_change_report.py 53`，再分别画出 `app.py` 和 `evaluation.py` 汇合到同一业务链的路径。

## 1. 区分两类输入

- 主程序的问题由谁决定：待填写。
- eval_cases.json 的问题为什么必须固定：待填写。
- 固定评测题能否替代用户自由体验：待填写。

## 2. 跟踪真实评测链

补全：

> eval_cases.json → load_cases → ______ → 真实检索和模型 → ______ + ______ → 退出码

- 答案正确但来源错误是否通过：待填写。
- 任一用例失败时评测程序应返回什么退出码：待填写。

## 3. 动手运行

先在主程序问一个自选问题，再运行独立评测程序：

| 模式 | 输入来源 | 实际结果 |
|---|---|---|
| 自由问答 | 用户现场输入 | 待填写 |
| 回归评测 | eval_cases.json | 待填写 |

## 4. 当天验收

- 评测程序是否调用正式 assistant：待填写。
- 报告能否区分 answer 与 citation 失败：待填写。
- 累计测试结果：待填写。
- Day54 应改善产品链中的哪一层：待填写。
