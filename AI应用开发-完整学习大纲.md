# AI 应用开发完整学习大纲（重排版）

> 创建：2026-06-14　｜　最近重排：2026-06-23
> 目标岗位：AI Agent 开发 / LLM 应用开发 / RAG 质量工程 / LLM 评测工程
> 差异化主线：**用测试工程背景做出"可测试、可评测、可回归、可上线"的 Agent 系统**
> 本文为唯一主计划（学习单元统一编号 Day 1-71，进度以仓库 dayNN 文件 + TASKS.md 为准）。
> **工期口径**：Day 1-71 是「纯学习日」编号（约 11-12 周纯学习时间），不含迭代、找工作、卡壳返工。文中出现的「70 天」一律指这 71 个学习单元，不是日历天；按验收推进、不绑日历，实际跨度会更长。

---

## 0. 怎么用这份文档

- 每个知识点带标签：
  - 【动手】= 必须写代码做出来
  - 【了解】= 能在面试讲清概念即可，**不要动手深学**（多为别的岗位的活）
- 核心原则：**深度优先 + 不跑偏**。判断任何新名词学不学，套一句话——
  > **"用模型" 就学；"造模型 / 改模型" 就跳过（只【了解】）。**

### 前置假设（开工前自查）

本计划假设你**已具备**：Python 基础、pytest 用过、Git 基本操作（commit/branch/PR）。这些是测试工程背景大概率有的，没有先补。

本计划**不假设**你会、会在用到的阶段现学：`async/await`（阶段4）、Docker（阶段4）、FastAPI（阶段4）、JWT/鉴权（阶段5）、SQL/数据库（阶段3 Text2SQL）。遇到时按阶段内的【动手】条目现学即可，不用提前囤。

### 本次重排的六条主轴（和旧版的关键区别）

1. **毕业项目从第一天就跑在真实脏语料上**：用你自己的真实数据（金融研报 / 自媒体素材 / 真实财报 PDF）当语料，不用干净示例文档。AI 应用最难的问题只在脏数据 + 真实提问里暴露，绕开它学两个月等于没碰真题。
2. **企业第一梯队特性并进毕业项目，不再当附录**：真实数据接入、文档级权限、认证多租户、CI 评测门禁这些"上岗即用"的东西，直接是毕业项目的组成部分，排在简历之前。
3. **评测做成独立主打作品，RAG + Agent 双评测**：护城河不是"跑一次出个数"，而是数据集版本化、回归曲线、prompt A/B、judge 与人工一致性、agent 轨迹评测。
4. **简历和面试后置到最后**：先把最硬的料（CI 门禁 + 评测平台 + 真实数据 + 真上线）做出来，简历才引用得到。
5. **边学边公开 + 早投简历**：每阶段结束 push GitHub + 发一篇自媒体；阶段3 做完就开始投，把面试当免费的 gap 诊断，别闷头学完 71 个单元再找工作。
6. **补结构化数据问答（Text2SQL）**：企业大量知识在数据库里、不在 PDF 里，阶段3 加一个 Text2SQL 工具分支，覆盖"对话式数据分析 / BI Agent"这类高频场景。

### 三条执行硬规则（必读）

1. **数据合规边界**：真实脏数据（付费研报、财报、含个人信息的素材）只在本地 / 私有环境跑，用于验证能力；**公网 demo 和公开仓库一律用脱敏或可公开的语料**。两套数据分开。把版权 / 隐私数据公开部署或喂第三方 API，是反护城河的减分项——你卖点正是企业数据边界，自己先别踩。
2. **时间盒（防无底洞）+ 降级路径**：每阶段设上限，到点冻结、剩下迭代往后排。尤其阶段2"做深"不等于无限做——先在约 1.5-2 周内冻结评测平台 MVP（够写进简历），再迭代。不绑日历，但要有上限。
   - **卡壳逃生阀**：任一【动手】条目卡超过约 1 天，先降级到能跑的最小版（如 reranker 换不通就先跳过、走召回 top-3），打个 `TODO` 标记继续往下，别在单点死磕。整段阶段卡超上限 1.5 倍，冻结当前成果、标记未完项、直接进下一阶段，回头再补——主线不断比单点完美重要。
3. **首个可投作品里程碑**：阶段2 结束的"RAG/Agent 评测平台"就是你第一个能独立写进简历、能投的作品。它优先作为主仓库里的 `evals/` + `reports/` 模块单独展示；如果拆独立仓库，也必须从主仓库同步抽出，不能维护两套分叉代码。阶段3 后"开始投简历"靠它，不是空手投；后续阶段把它扩成完整毕业项目。

### 第一性原则验收：企业为什么会录用

企业招 AI 应用开发，不是为了找一个"会跑模型 demo"的人，而是为了把不稳定的模型能力放进业务系统里，并且能持续维护。最终能力要落到四件事：

1. **能接真实业务数据**：文档脏、格式乱、权限复杂、会更新。
2. **能证明效果变好**：有评测集、指标、失败用例、回归曲线，不靠手工试几句。
3. **能在团队流程里交付**：PR、review、CI、测试、配置、部署都有证据。
4. **能处理上线约束**：权限、多租户、成本、延迟、监控、安全、合规都能说清并跑出最小实现。

因此本计划的成功标准不是 Day71 打勾，而是拿出一个可运行、可评测、可回归、可上线、可讲清 tradeoff 的项目。缺任何一项，都还停在 demo 层。

### 通过标准（学完后的最低交付物）

- 一个 GitHub 主仓库：`src/`、`tests/`、`evals/`、`reports/`、`docs/`、`.env.example`、README、架构图、演示截图齐全。
- 一个线上地址：企业知识库 Agent 能问答，支持引用、权限过滤、多租户隔离（线上用可公开语料，真实脏数据本地跑）。
- 一个评测平台：50+ 条 RAG/Agent 评测集，含事实、跨文档、拒答、引用准确性、工具轨迹；有失败用例库和回归曲线。
- 一条 CI 证据：改坏 prompt 或 retriever 后，GitHub Actions 评测红灯，PR 被拦。
- 一套诊断证据：LangSmith trace 或等价日志 + p95/p99 + 错误率 + token 成本，能定位失败原因。
- 一轮改进闭环证据：从失败用例库挑一个 case → 改 → 评测验证变好 → 回归曲线证明没弄坏别的，全程留痕。
- 一组成本×质量 tradeoff：缓存 / 压缩 / 小模型路由优化后，量化"省 X% 成本、质量掉 Y%"。
- 一份求职材料：简历、5 分钟项目讲解、面试问答都引用同一批真实证据，不写空泛能力。

这些交付物是就业门槛，不是加分项。

---

## 1. 岗位边界：学什么、不学什么 ⭐先读这节

很多 AI 名词属于**另一个岗位**。混学只会拖慢转行，且你拼不过科班。先划清：

|     | AI 应用开发工程师（你的目标）           | 大模型算法 / 推理 infra 工程师（另一条路）     |
| --- | -------------------------- | ------------------------------ |
| 干什么 | 用模型搭应用：RAG、Agent、工作流、评测、上线 | 训模型、压模型、加速推理                   |
| 门槛  | Python + 工程 + 评测质量         | 深度学习 + CUDA + 分布式（多为 CS/ML 硕博） |

**明确不学清单（只【了解】是什么，绝不花时间深学）：**
- 模型蒸馏、量化原理（GPTQ/AWQ/GGUF 内部）、Flash Attention / 推理加速 kernel
- 微调的多租户 / SaaS 个性化训练架构（注：LoRA 微调本身只在 §6 跑一次建体感，不深学训练架构，二者不冲突）
- EINO（字节 Go 框架）—— 除非你明确投字节系/Go 栈公司，否则拿到面试再补
- A2A（Agent 间通信协议）—— 前沿，能聊即可

---

## 2. 能力地图总览

| #   | 能力模块                             | 阶段      |
| --- | -------------------------------- | ------- |
| M1  | LLM API 与调用基础                    | 前置基础    |
| M2  | Prompt 工程与结构化输出                  | 前置基础    |
| M3  | RAG 基础（加载/切割/向量/检索/生成）+ 裸写理解 harness | 前置基础 |
| M4  | RAG 进阶（真实脏数据 + 召回优化）             | 阶段1     |
| M5  | RAG + Agent 双评测（护城河，主打作品）       | 阶段2     |
| M6  | Agent / LangGraph + 任务规划 + MCP   | 阶段3     |
| M7  | 工程化与可观测                          | 阶段4     |
| M8  | 毕业项目整合（真实数据/权限/多租户/CI门禁/真上线）★上岗即用 | 阶段5  |
| M9  | 上线补全（监控/生产向量库/合规/供应商）           | 阶段6     |
| M10 | 求职（作品/简历/面试，后置）                  | 阶段7     |
| M11 | 认知层（微调取舍/原理/推理框架，多为【了解】）         | 阶段4 末集中过一遍，见 §6 |

---

## 3. AI Agent 主要应用场景（知道自己在为什么做项目）

> 实话：JD 写"Agent"的岗位八成实际做"RAG + 工具调用 + 工作流"，全自主 Agent 生产里还不多。偏确定性的那部分恰好对你测试背景有利。

按落地频率：
1. **企业知识库 / 文档问答（RAG）** —— 最多，毕业项目主打
2. **智能客服 / 客户支持 Agent** —— RAG + 工单 + 转人工
3. **对话式数据分析（Text2SQL / BI Agent）**
4. **研发提效 Agent**（代码审查、测试生成、PR 总结）—— 你测试背景契合
5. **内部流程自动化**（审批/报销/合同/工单）—— 偏确定性
6. **垂直行业 Copilot**（法律/医疗/金融/教育）
7. **调研 / 报告生成 Agent**
8. **Multi-agent 编排**（前沿，生产早期）

场景 1/2/5/6 上线后最痛的都是答错、幻觉、回归、监控 —— 即评测与质量，正是你的★护城河。

---

## 4. 分阶段详细计划

> 节奏：按验收标准推进、不绑日历，一天能吃 2-3 个学习单元就吃。每阶段末有验收标准，不达标不往下走。
> **快慢分阶段，别匀速推到底**：
> - 🟢 可加速：阶段0 固本 + 阶段1 RAG 进阶——"学了就会"的活，约 1-2 周吃完。
> - 🔴 要深耕：阶段2 评测 + 阶段3 Agent——护城河，把省下的时间投进去做深。
> - 🟡 按产出验收：阶段4 工程化 + 阶段5 毕业项目 + 阶段6 上线补全——质量决定面试成败，慢下来是对的。
> **公开节奏**：阶段1/2/3/5 结束各 push 一次 GitHub + 发一篇自媒体；阶段3 验收后开始投简历，把面试当 gap 诊断。

### 阶段 0 ｜固本 + 裸写理解 harness（约 1 周）　M1-M3

**目标**：把"会抄"变成"会写 + 懂为什么"；并亲手写一次没有框架的 RAG / agent loop，看清框架替你包掉的那层（harness）长什么样。

- 【动手】基础调用 + Prompt + LCEL 管道：`prompt | model | parser` 组最小链
- 【动手】控制输出：temperature 对比 + 给 chain 调 `.stream()` 流式
- 【动手】结构化输出：`with_structured_output` + Pydantic
- 【动手】重写"带记忆的对话"，搞懂记忆原理（模型本身无记忆 / session_id / 记忆存哪）
- 【动手】工具调用：定义 tool、模型决定何时调用、回填结果作答——为 Day10 裸写 agent loop 打底
- 【动手】综合小项目：记忆 + 工具 + 多角色组合成一个对话应用
- 【动手】重写"最小 RAG"（加载/切割/向量/检索/生成 + MMR / 拒答），搞懂每个零件 + RunnablePassthrough 怎么组链
- 【动手】**提示工程进阶**：zero-shot / few-shot / CoT 各写对比版
- 【动手】**裸 SDK 手写一次**：不用 LangChain，直接用模型 SDK 手写"检索→拼 prompt→生成"的 RAG 循环和"思考→调工具→观察"的 agent loop，再和框架版对比——理解 harness = 模型之外那层工程
- 【了解】**LLM 原理认知层**：token、embedding 为什么能算语义、attention 一句话、为什么会幻觉

**验收**：框架版和裸写版都能跑通并讲清每个零件；能说清"框架替我做了什么"；原理 4 问能口头讲清。

---

### 阶段 1 ｜RAG 进阶（约 1 周）　M4　🟢 可加速

> **从这里开始换真实脏语料**：用你自己的金融研报 / 自媒体素材 / 真实财报 PDF 当语料，刻意挑带表格、扫描件、页眉页脚、多版本重复的脏文档。后面所有阶段都在这批语料上滚。

**目标**：从"会搭 RAG"到"会让它在脏数据上检索得准"。

- 【动手】文档解析质量：PDF 表格/页眉页脚/换行噪声处理（对比 PyPDFLoader / pypdf / Unstructured）
- 【动手】chunk 策略对比：固定长度 vs 按标题/语义切；overlap 真实作用
- 【动手】metadata 打标签 + 检索时按 metadata 过滤
- 【动手】**混合检索**：向量召回 + BM25 关键词召回（解决专有名词/编号召回不准）
- 【动手】**查询改写**：multi-query，对比开关前后召回；【了解】HyDE（论文热、生产落地少，能讲清"为什么生产不常用"即可）
- 【动手】**Context Engineering**：上下文裁剪 / 压缩 / 排序（2026 高频 JD 词）
- 【动手】**专业向量库**：FAISS 换 **Chroma**（带持久化）跑通；【了解】pgvector / Milvus / Pinecone 选型
- 【了解】reranker：召回 20 → 重排取 top 3，何时需要
- 【了解】**LlamaIndex**：和 LangChain 的定位区别（RAG 场景常并列，面试能比较）
- 【动手】**多模态输入**（轻量）：让应用能读图片/扫描件 PDF（视觉模型）

**验收**：能说清"答错是检索没召回 vs 召回了生成错"，并动手改善召回；FAISS→Chroma 持久化跑通；语料是真实脏文档。公开笔记和截图只用可公开语料，真实脏数据只保留本地结果。【公开：push 阶段成果 + 一篇笔记】

---

### 阶段 2 ｜RAG + Agent 双评测（约 1.5-2 周）　M5　★护城河，主打作品

**目标**：把评测做成一个能单独展示的主打作品，不是"跑一次 RAGAS 出个数"。普通开发不会，你测试背景核心卖点全在这。

基础评测：
- 【动手】造评测集：问题 + 标准答案 + 应命中来源；三大指标：检索相关性、答案正确性、忠实度（groundedness）
- 【动手】引用准确性：答案引的来源是否真支持该结论
- 【了解】LLM-as-judge 原理与坑
- 【动手】**业界工具**：RAGAS + DeepEval 跑一遍；接 LangSmith evaluation 看每条 trace；【了解】HaluEval / TruthfulQA 幻觉基准
- 【动手】给应用造 **50+ 条评测集**：事实/跨段落/**拒答**/引用准确

评测工程化（旧版缺，本次补深）：
- 【动手】**评测集版本化 + 回归曲线**：评测集进 git，每次改 prompt/chunk 跑一遍，画出指标随 commit 的回归曲线
- 【动手】**prompt A/B 对比**：两版 prompt 在同一评测集上对比打分，出对比表
- 【动手】**judge 一致性校验**：抽样人工标注，校验 LLM-judge 和人工打分的一致性，知道 judge 自己有多准
- 【动手】**Agent 评测**：轨迹（trajectory）评测、工具调用正确性、任务完成率——评测对象不止 RAG，也评 Agent（这一块旧版几乎是空的，面试官一问就漏）

产物：
- 【动手】出 **RAG/Agent 评测报告**（指标 + 失败用例分析）+ 建"失败用例库"归档答错 case
- 【动手】评测维度除质量外，记录每条的 **token 成本与延迟**，作为阶段4 成本优化的基线
- 【动手】评测做成**可展示看板**（截图/演示），面试直接拿出"幻觉率 X%、失败分析"

**验收**：评测平台能单独 demo；拿得出带计算方法的数字（"拒答召回率 X%、幻觉率 Y%，改 chunk 后到 Z%"）；能评 agent 轨迹，不止评 RAG；评测集 = agent loop 的反馈信号，能讲清。【公开：评测平台作为 GitHub 主打模块 / 项目】

---

### 阶段 3 ｜Agent / LangGraph + 任务规划（约 2 周）　M6

**目标**：从手写 tool 循环升级到有状态、可循环、可中断、会规划的 Agent。开篇先用 **loop / harness 框架**定调（agentic loop = ReAct / plan-act-observe；harness = 模型之外的工程层），再讲具体技术。

阶段 3a — LangGraph 基础：
- 【动手】State（TypedDict+Annotated）、Node、Edge、conditional_edges、循环、`recursion_limit` 防死循环
- 【动手】用 LangGraph 重写阶段0 的工具调用对话（对比手写循环）

阶段 3b — 任务规划：
- 【动手】**ReAct**（边想边做）用 LangGraph 实现
- 【动手】**Plan-and-Execute**（先规划再执行）
- 【了解】Reflexion / ReWOO / Tree of Thoughts 各自适用场景

阶段 3c — 状态/安全/集成：
- 【动手】checkpoint / persistence：状态持久化、中断恢复
- 【动手】**上下文管理**：长对话/长文档裁剪、摘要压缩、只留最近 N 轮 + 历史摘要
- 【动手】streaming 流式中间步骤
- 【动手】**human-in-the-loop**：`interrupt` 高风险动作前等人工确认
- 【动手】工具调用安全：禁止真实删除/发送/支付权限
- 【动手】**Text2SQL 工具分支**：给 Agent 加自然语言→SQL→查库（SQLite/PG）→作答的工具，覆盖"对话式数据分析 / BI Agent"场景；安全做只读连接 / 限制表 / 校验 SQL；能讲 RAG（非结构化）vs Text2SQL（结构化）怎么分流
- 【动手】**MCP（模型上下文协议）**：用 MCP 接一个标准化工具/数据源
- 【了解】A2A 概念；【了解】AutoGen / CrewAI（multi-agent 框架，和 LangGraph 的定位区别）

- 【动手】项目：搜索 + 总结 Agent（DuckDuckGo + HITL + 轨迹日志），**接阶段2 的 agent 评测跑一遍**

**验收**：能讲清 Chain vs LangGraph、状态持久化、防死循环、HITL、ReAct vs Plan-Execute、RAG vs Text2SQL；MCP 接通一个工具；agent 能被评测覆盖。【公开 + 开始投简历，把面试当 gap 诊断】

---

### 阶段 4 ｜工程化与可观测（约 1.5 周）　M7

**目标**：摆脱"教程使用者"，变成工程师。

- 【动手】服务化：FastAPI 把 RAG/Agent 包成 HTTP API
- 【动手】async/await、httpx 异步
- 【动手】可靠性：超时、重试（指数退避）、限流、provider fallback
- 【动手】**成本优化**：token 统计 + 缓存（含语义缓存）+ prompt 压缩 + model routing；拿阶段2 成本基线量化"省 X% 成本、质量掉 Y%"（用评测集验证质量没塌）
- 【动手】数据持久化：SQLite/PostgreSQL 存对话/用户
- 【动手】**安全 / guardrails**：prompt injection 防护、PII 脱敏、输出内容审查、密钥走 `.env`
- 【动手】**自动化测试**：pytest 对 prompt/检索/答案格式做回归测试（接阶段2 评测集）
- 【动手】**可观测**：LangSmith trace 定位检索失败/幻觉/prompt 退化；【了解】Langfuse（开源可自部署，数据不出域，国内生产环境常用替代，JD 出现频率在涨）
- 【动手】Docker 基础部署
- 【了解】**推理框架**：Ollama 本地跑一次开源模型；知道 vLLM/TGI/SGLang 做什么

**验收**：应用有 FastAPI 接口 + pytest 回归集 + LangSmith trace + `.env` 密钥 + 能 `docker run`。

> **认知层在这里插入（约 0.5-1 天）**：阶段4 验收后、进阶段5 毕业项目之前，集中过一遍 §6 的认知层模块（微调取舍 / LoRA 体感 / 量化蒸馏一句话 / 5 类输出方式）。放这是因为前面工程能力已成型，回头看原理取舍最有体感，又不耽误主线。

---

### 阶段 5 ｜毕业项目整合（含企业第一梯队 + 真上线）（约 2.5 周）　M8 ★上岗即用

**目标**：把前面所有东西整合成一个**跑在真实数据上、带企业第一梯队特性、有公网地址**的毕业项目。这是"通过面试"和"企业上手"之间最大的差，也是简历最硬的料——所以排在简历之前。

> 旧版把这些企业特性放在毕业项目和简历之后（附录式补全），导致你拿去面试的是个跑在干净 PDF 上的玩具版。本次全部并进毕业项目。每个条目是多日单元，不是 1 天。

- 【动手】项目架构 + **Git 协作工作流**：feature 分支 / commit 规范 / PR 流程（不直接推 main）/ 自己 review 自己的 PR
- 【动手】RAG + Chroma 持久化 + 评测集接入（真实语料）
- 【动手】Agent + MCP + Text2SQL 工具 + HITL 审批节点 + **agent 评测**接入
- 【动手】**真实数据接入 + 增量同步**：接 Notion/DB/文件夹 connector，文档增删改时向量库增量维护，不全量重建
- 【动手】**文档级权限过滤**：按用户身份过滤检索结果（权限在检索层做，不靠 prompt 约束）—— 企业 RAG 最痛、最常被问
- 【动手】**认证 + 多租户 + 限流**：FastAPI JWT/API key 鉴权、按租户隔离数据
- 【动手】工程化接入：重试/缓存/安全/pytest 回归/trace
- 【动手】**CI 评测门禁**★：GitHub Actions 每次 PR 跑 pytest + 评测集，幻觉率/召回率跌破阈值就拦 merge —— 你护城河最硬的延伸，故意改坏一个 prompt 看 CI 红
- 【动手】端到端联调：上传→问答→引用→评测→trace 全流程跑顺，修 bug
- 【动手】**一轮改进闭环**：从失败用例库挑一个 case → 改 prompt/chunk/检索 → 评测验证变好 → 回归曲线证明没弄坏别的，全程留痕（面试可讲）
- 【动手】**真部署**：上云主机 / Fly / Render，拿到公网 URL，手机能访问（线上用可公开语料；`docker run` 在本机 ≠ 上线）
- 【动手】GitHub 打磨：README + 架构图 + 截图 + 演示 GIF/视频 + 评测看板截图

**验收**：CI 评测门禁能拦坏 PR；同一问题不同用户拿到不同来源（权限生效）；真实脏语料本地能问答、公网用可公开语料；有改进闭环记录；有一个能点的公网地址。【公开：主打作品成型】

---

### 阶段 6 ｜上线补全（约 1.5 周，按目标公司取舍）　M9

**目标**：补生产运维 / 合规 / 供应商这些"上线才碰得到"的事。按目标公司类型取舍——国内必做合规，外企偏 Azure。

- 【动手】**生产监控告警**：p95/p99 延迟、错误率、成本趋势看板 + 告警阈值（不止单条 trace）
- 【动手】**生产级向量库**：Chroma → pgvector 真跑一遍 + 选型理由；迁移后**毕业项目默认切到 pgvector**（生产最常见选型：复用已有 Postgres，少维护一个组件），Chroma 留作对比讲 tradeoff；【了解】Milvus / Pinecone 何时才需要
- 【动手】**内容安全合规审核**（国内上线硬门槛）：输入输出过滤；【了解】备案 / 内容安全要求
- 【动手】**模型供应商抽象**：支持切 OpenAI / Azure OpenAI / Bedrock；【了解】私有化部署场景
- 【动手·轻量】**负载压测 + OpenAPI 版本**：locust 在真部署上压测看并发拐点；接口加 `/v1`

**验收**：有整体健康度看板；pgvector 跑通；能挡违规输入/输出；改配置就能切供应商；能讲"系统大概扛多少并发、瓶颈在哪"。

---

### 阶段 7 ｜求职冲刺（约 1 周，后置）　M10

> 后置的理由：简历要引用你最硬的料（CI 门禁 + 评测平台 + 真实数据 + 真上线），这些到阶段5-6 才成型。提前写简历只能写玩具版。注意：阶段3 后已经开始投简历拿反馈，这里是收尾冲刺，不是第一次找工作。

- 【动手】简历：按"测试转 AI 质量/评测工程"定位（模板见 §5），引用 CI 评测门禁、评测平台、真实数据、真上线
- 【动手】面试题准备（§5 清单）：RAG/Agent/评测/工程/认知
- 【动手】练 5 分钟项目讲解（讲 tradeoff）
- 【动手】1-2 次完整模拟面试 + 复盘，找薄弱点回对应阶段补

---

## 5. 求职材料

### 简历项目描述模板

> **简历分两版，别用没做完的东西吹牛**：
> - **投递期版本（阶段3 后开始投用）**：只写已经做完的——评测平台、真实数据验证、Agent + 评测、CI 门禁。**此时还没真上线，不写「已部署上线」「公网地址」「生产监控 p95/p99」**，否则面试一问就穿帮。
> - **完整版本（阶段5-6 后用）**：补上「真上线 + 公网地址 + 权限多租户 + 生产监控」。
> 下面是完整版本；带 🔶 的行是阶段5-6 才能写的，投递期先删掉。

```text
【企业知识库 Agent 与自动化评测平台】
基于 LangChain + LangGraph 构建文档问答 Agent：本地用真实脏语料验证企业场景。🔶公网 demo 使用可公开语料并已部署上线。
- 🔶真实数据 connector + 增量同步；文档级权限过滤（检索层），多租户隔离
- 向量检索 + metadata 过滤 + 混合检索改善专有名词召回；Chroma 持久化；扫描件/图片走视觉模型解析（多模态）
- LangGraph 实现状态持久化、MCP 工具调用、Text2SQL 结构化数据问答与 human-in-the-loop 审批节点
- 构建 50+ 条 RAG/Agent 评测集（事实/跨文档/拒答/引用/轨迹），评测集版本化 + 回归曲线 + prompt A/B
- CI 评测门禁：GitHub Actions 在 PR 上跑 pytest + 评测，幻觉率/召回率跌破阈值拦 merge
- 缓存 + model routing 降本，量化成本×质量 tradeoff；LangSmith trace 区分检索失败与生成幻觉；🔶生产监控 p95/p99 + 成本看板
```

### 必答面试题
- RAG 为什么会答错？怎么判断是检索错还是生成错？
- chunk_size 怎么选？不同文档怎么处理？怎么构造 RAG 测试集？怎么评 Agent（轨迹）？
- 什么时候走 RAG、什么时候走 Text2SQL？Text2SQL 怎么防注入？
- LangGraph vs Chain？状态怎么持久化？怎么防死循环？高风险工具怎么 HITL？
- ReAct 和 Plan-and-Execute 区别？MCP 解决什么问题？harness 指什么？
- 文档级权限为什么要在检索层做、不能靠 prompt？多租户怎么隔离？
- RAG vs 微调怎么选？怎么降低幻觉？prompt 改了怎么回归？怎么在不掉质量的前提下降本？线上幻觉率/成本怎么监控？

### 不要踩的坑
- 不声称"懂大模型底层原理"除非真懂 transformer/attention
- 不只展示聊天框；不说"准确率提升40%"却拿不出评测集和算法
- 不把 key 写进代码/README；不给 Agent 真实删除/发送/支付权限

---

## 6. 认知层模块（M11，以【了解】为主，别陷进去）

**固定插入点：阶段4（工程化）验收后、阶段5（毕业项目）开始前，集中过一遍（约 0.5-1 天）。** 放这是因为工程能力已成型，回头看原理取舍最有体感，又不打断主线：

- 【了解】**微调取舍**：RAG 适合知识常更新+要溯源+成本低，微调适合固定风格/术语但贵且知识静态
- 【动手·一次性】跑一遍 **LoRA 微调**（现成脚本/Colab，建立体感即可）+ 能讲清"RAG vs Prompt vs 微调"取舍
- 【了解】量化 / 蒸馏 / Flash Attention / Scaling Laws —— 各记一句话定义，不学原理
- 【了解】大模型 5 类输出/交互方式：普通文本、流式、结构化 JSON、tool-calling、多模态

---

## 7. 毕业项目规格：企业知识库 Agent + 自动化评测平台（真实脏语料本地验证 + 可公开语料真上线）

| 能力 | 功能 |
|------|------|
| 数据 | 真实脏语料（金融研报/自媒体素材/财报）；真实 connector + 增量同步 |
| 文档处理 | 上传 PDF/Word/Markdown（含表格/图片），多文档检索；**扫描件/图片走视觉模型解析**（多模态输入，承接阶段1） |
| 问答 | RAG 检索 + 引用来源标注 + 多轮对话；结构化数据走 Text2SQL |
| 权限 | 文档级权限过滤（检索层）、认证、多租户隔离 |
| Agent | LangGraph 状态管理、MCP 工具、Text2SQL 工具、高风险动作 HITL 审批 |
| 评测★ | 50+ 条 RAG/Agent 回归集、评测集版本化、回归曲线、prompt A/B、judge 一致性、看板 |
| 可观测 | LangSmith trace、失败用例归档、p95/p99 + 成本监控 |
| 工程 | FastAPI、pytest 回归、CI 评测门禁、Chroma/pgvector、Docker、公网部署 |

技术栈：LangChain + LangGraph + MCP + Chroma/pgvector + FastAPI + SQLite/PG + pytest + GitHub Actions + LangSmith

---

## 8. 资源清单

> 这些工具迭代很快，链接和用法**一律以官方最新文档为准**；本清单只给入口，不锁版本。

官方文档：
- LangChain：https://python.langchain.com/docs/introduction/
- LangGraph：https://langchain-ai.github.io/langgraph/
- LangGraph 快速上手（quickstart）：https://langchain-ai.github.io/langgraph/tutorials/introduction/
- LangSmith（trace + eval）：https://smith.langchain.com/
- RAGAS（RAG 评测）：https://docs.ragas.io/
- DeepEval：https://docs.confident-ai.com/
- MCP（模型上下文协议）：https://modelcontextprotocol.io/
- MCP server 示例 / SDK：https://github.com/modelcontextprotocol/servers
- Chroma：https://docs.trychroma.com/　｜　pgvector：https://github.com/pgvector/pgvector

视频 / 社区：
- YouTube：LangChain 官方频道 "LangGraph" 系列
- 中文社区：LangChain 中文文档与公众号、Datawhale 相关开源教程（搜最新版，旧教程 API 易过期）

---

## 9. 护城河叙事：harness engineering（贯穿全程）

- 行业共识：**Agent = Model + Harness**——"模型是 agent 系统里最小的部分，harness（工具调用解析、记忆、可靠性、安全、评测）才决定生产可靠性"。由此出现新学科 **harness engineering**。
- 这门学科正好覆盖你的工程 + 质量 + 评测能力。简历与面试用 **harness engineering** 定位"测试转 AI 的差异化"，比单说"会 RAG"有辨识度。阶段0 的裸写、阶段2 的评测、阶段5 的 CI 门禁，都是 harness 的具体证据。
- **agentic loop**（推理→行动→观察，ReAct 是经典实现）：把你的测试背景和 loop 显式挂钩——评测集 = loop 的反馈信号。

### 简历 JD 关键词清单（确保每个都能讲清）

RAG、Agent、LangChain / LangGraph、Tool Calling、**Text2SQL**、Prompt Engineering、**Context Engineering**、**MCP**、**agent harness / harness engineering**、**agentic loop / ReAct**、RAGAS / DeepEval / 评测 / 幻觉率、agent 轨迹评测、CI 评测门禁、文档级权限 / 多租户、**成本优化（缓存/路由）**、LLMOps / 可观测、pytest 回归、LoRA 微调取舍、LlamaIndex / AutoGen（了解级）。
