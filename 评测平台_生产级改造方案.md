# 自动评测平台 · 生产级改造方案

> 目标：把 Day51+ 毕业项目里的"自动评测"从**能跑的 demo**升级成**能写进简历、对得上企业生产标准**的评测/质量工程平台。
> 你的护城河是"测试 + 评估"，评测平台就是简历第一主角，必须真做扎实。
> 路线：自研轻量指标（可控、面试好讲） + 成熟框架（Ragas/DeepEval）接一层证明"业界对齐"。

---

## 一、一句话诊断

**好零件已经有，但没有装进"产品"。**

`Day19`（LLM-judge + 忠实度）、`Day24`（judge 一致性校验）、`Day26`（DeepEval 三维诊断）这些**真正拿得上台面的能力**，现在只是独立的学习文件；而真正会被写进简历、被面试官 clone 来看的 **capstone 本体评测**（`capstone/evaluation.py` + `capstone/ci_gate.py`）用的还是**关键词字符串匹配 `if k in ans`**，Agent 评测用的是**手写死的 `DEMO_TRAJECTORIES`**。

面试官一眼会看到的三处"self-证明循环"：

1. **指标是字符串匹配**：`if "检索" in ans` 判对错——答案换个说法就误判，测不了语义、测不了幻觉。
2. **Agent 轨迹是编的**：`DEMO_TRAJECTORIES` 是人手写的理想轨迹，不是真实 agent 跑出来的，等于"自己出题自己答自己判对"。
3. **offline 演示答案 + 假成本**：`offline_answer()` 返回写死的答案，`estimate_cost()` 用"2字符≈1token"——demo 链路无所谓，但不能是简历的主指标。

**结论**：不是推倒重来，是**把已有的强能力接进 capstone 主线，再补三块企业真在做、而你测试背景刚好能碾压的东西**：LLM-judge 一致性、统计显著性、真实轨迹捕获。

---

## 二、现状盘点：留什么 / 改什么 / 删什么

| 模块 | 现状 | 判定 | 动作 |
|------|------|------|------|
| `capstone/evaluation.py` | 关键词命中 + 拒答字符串检测 | ⚠ 太弱，是简历主指标却最薄 | **重写为分层指标**，复用 Day19/26 |
| `capstone/ci_gate.py` | 阈值门禁（结构对） | ✅ 结构好 | 保留，喂给它的指标升级即可 |
| `capstone/test_regression.py` | pytest 参数化回归 | ✅ 护城河亮点 | 保留，加 judge 断言 + 多次采样 |
| `evals/run_eval_platform.py` | 质量+成本+延迟+历史CSV | ✅ 平台骨架好 | 保留骨架，指标换真、成本换 tiktoken |
| `evals/agent_trajectory_eval.py` | `DEMO_TRAJECTORIES` 手写轨迹 | ❌ 最扣分 | **改成捕获真实 LangGraph 轨迹** |
| `day19_eval_llm_judge.py` | LLM-judge 正确性/忠实度 | ✅ 强能力，但没接主线 | **提升为 evaluation.py 的核心指标** |
| `day24_prompt_ab_judge.py` | judge 一致性 + prompt A/B | ✅✅ 尖刀能力 | **前置成平台一等公民**（见 P1） |
| `day26_eval_report_failures.py` | DeepEval 三维失败诊断 | ✅✅ 业界对齐 | **接进 CI 门禁的证据链** |
| `eval_set.json`（capstone） | 6 条，只有 keywords | ⚠ 太少、无难度分层 | **升级为 golden dataset**（见 P4） |

删/收敛：根目录散落的 `venv_*.log`、重复的 eval_set（`eval_set.json`/`eval_set_full.json`/`evals/eval_cases.json` 三份）——**收敛成一份带 schema 的 golden set**，其余降级为历史。

---

## 三、对标企业生产标准的差距

企业 2024-2025 做 RAG/Agent 评测的实际标准，和你现状的差距：

| 维度 | 企业标准做法 | 你现状 | 差距 |
|------|------------|--------|------|
| 检索质量 | Context Recall / Precision、命中率@k | 无（只看最终答案） | **缺整层** |
| 生成质量 | Faithfulness（防幻觉）、Answer Relevancy、语义正确性 | 关键词匹配 | 有零件(Day19/26)未接入 |
| 裁判可信度 | LLM-judge + 人工标注一致性(kappa) + 偏置控制 | 有(Day24)未接入 | **接进来就是尖刀** |
| 统计严谨 | 多次采样、通过率置信区间、方差报告 | 单次跑、点估计 | **缺，且是你最该有的** |
| Agent 评测 | 捕获真实轨迹后评 | 手写轨迹 | **必须换真** |
| 数据集治理 | 版本化、难度分层、对抗样本、数据来源 | 6条平铺 | 缺治理 |
| 成本/延迟 | tiktoken 真实 token + 真实价格、p95/p99 | 字符估算 | 换真即可 |
| 门禁 | 质量+成本双门禁、按维度阈值 | 单质量阈值（结构好） | 小补 |

**最该优先补、且和测试背景最搭的三件**：`judge 一致性`、`统计显著性`、`真实轨迹`。这三样大多数转行候选人都没有，是你区别于"只会调 LangChain 的人"的证据。

---

## 四、改造后的目标形态

```
评测平台（capstone/eval/ 或 evals/）
├── 分层指标
│   ├── 检索层：context_recall / context_precision / hit@k
│   ├── 生成层：faithfulness / answer_relevancy / correctness(LLM-judge)
│   └── 安全层：拒答正确率 / 注入抵抗 / PII 泄漏
├── LLM-as-judge（做对版）
│   ├── rubric 打分（1-5 带标准）+ 结构化输出
│   ├── 偏置控制：pairwise 位置交换、reference-guided
│   └── judge 一致性：对人工标注算 Cohen's kappa / 一致率 ★
├── 统计层
│   ├── 每条用例跑 N 次，报通过率 + 置信区间(bootstrap) ★
│   └── 回归对比：新旧版本差异是否显著（不是噪声）
├── 真实 Agent 轨迹
│   └── 捕获 LangGraph 真实 tool_calls → 评工具正确/禁用/完成/步数 ★
├── 数据集治理：golden_set.json（版本号、难度、来源、对抗标记）
├── 框架对齐：Ragas / DeepEval 各接一条，跑同一 golden set 对照
├── 报告：dashboard.html（回归曲线 + 维度雷达 + 失败库）
└── CI 门禁：质量 + 成本双门禁，跌破阈值 exit 1 拦 PR
```

---

## 五、改造路线图（按优先级，2-3 周可完成）

每个 P 都标了**落点文件**和**面试一句话价值**。

### P0 · 指标分层（把 Day19/26 接进 capstone 主线）★最高优先
**做什么**：`capstone/evaluation.py` 的每条用例，除了现有的拒答/关键词（保留为便宜的一级信号），补上：
- 生成层：调用 Day19 的 judge 打**忠实度 + 正确性**（1-5，结构化输出，temperature=0）。
- 检索层：用 `kb.retriever.invoke(q)` 拿到真实召回，算 `context_recall`（参考答案的关键信息有没有被召回）、`hit@k`。
- 每条用例产出一个**多维打分对象**，而不是单个 pass/fail。

**落点**：改写 `capstone/evaluation.py`；复用 `day19_eval_llm_judge.py` 的 `Verdict` + judge_llm；复用 `day26` 的 DeepEval 维度。
**面试话术**："我的评测不是关键词匹配，是检索层+生成层+安全层三层指标，生成层用 LLM-as-judge 打忠实度和正确性，检索层单独量化召回，这样失败能定位到是检索没召回还是生成幻觉。"

### P1 · LLM-judge 做对 + 一致性校验 ★护城河尖刀
**做什么**：把 `day24` 的 judge 一致性从"学习文件"提成平台一等能力：
- eval_set 里已有 `manual_score`（人工分），让 judge 对同一批打分，算 **Cohen's kappa / 一致率**，写进报告。
- judge 低于一致性阈值时，报告里标注"该指标本轮不可信"——**诚实地承认裁判会错**，这正是测试思维。
- pairwise 场景（prompt A/B）做**位置交换**去除位置偏置。

**落点**：新增 `capstone/eval/judge.py`（判分 + 一致性）；`day24` 逻辑迁移收敛。
**面试话术**："LLM 裁判本身会有偏差，我用人工抽样标注对它做一致性校验（kappa=0.x），低于阈值就不采信这个指标，还做了位置交换去偏置——不是盲信大模型打的分。"
**为什么是尖刀**：90% 转行候选人只会"让 GPT 打个分"，能说清 judge 可信度校验的，直接进入"质量工程师"档位。

### P2 · 统计显著性 ★测试背景杀手锏
**做什么**：LLM 输出有随机性，单次跑的通过率是噪声。
- 每条用例（或整集）跑 **N 次**（N=5~10），报**通过率 + 95% 置信区间**（bootstrap 或 Wilson 区间）。
- 版本回归时，判断"新版比旧版好"是**统计显著**还是波动（配对比较）。
- 报告里通过率写成 `82% [76%, 88%]` 而不是裸 `82%`。

**落点**：`capstone/eval/stats.py`（bootstrap/Wilson + 显著性）；接进 `run_eval_platform.py` 和 CI 门禁。
**面试话术**："LLM 是随机系统，我不看单次通过率，跑 N 次报置信区间，回归时用配对检验判断改动是真的变好还是噪声——这是我测试背景直接迁移过来的。"

### P3 · 真实 Agent 轨迹捕获 ★干掉最扣分项
**做什么**：删掉 `DEMO_TRAJECTORIES`，改成：
- 跑真实 `capstone/agent.py`（LangGraph），从 `stream`/`messages` 里**捕获真实 tool_calls** 落盘成轨迹。
- 再用现有 `evaluate_trajectory` 评：期望工具调了没、禁用工具（如 delete_database）碰了没、是否完成、步数是否合理。
- 保留少量固定轨迹**只作 offline CI 冒烟**，主指标必须来自真实运行。

**落点**：`evals/agent_trajectory_eval.py` 加 `capture_trajectory(agent, case)`；`capstone/agent.py` 暴露可捕获接口。
**面试话术**："Agent 评测我评的是真实跑出来的轨迹，重点看有没有碰高危工具、有没有绕远路死循环，不是只看最终答案对不对。"

### P4 · Golden Dataset 治理
**做什么**：三份 eval_set 收敛成一份 `golden_set.json`，每条带：
- `id / type / question / reference / keywords / expected_sources / should_refuse / manual_score`
- 新增：`difficulty`（easy/medium/hard）、`source`（数据来源/是否可公开）、`adversarial`（是否对抗/注入样本）、`version`。
- 补 **10-15 条对抗样本**：prompt 注入、越权提问、诱导幻觉、跨文档推理——安全层就靠这批。

**落点**：`data/golden_set.json` + `capstone/eval/dataset.py`（加载/校验 schema/按难度分层统计）。
**面试话术**："评测集是版本化资产，分难度、带对抗样本，能按难度看模型在 hard case 上掉多少。"

### P5 · 成本/延迟真实化
**做什么**：`estimate_cost` 换 **tiktoken 真实分词** + 真实模型价格；延迟报 **p50/p95/p99** 而非均值。接 `capstone/monitoring.py`。
**面试话术**："成本和延迟都是真实 token 和 p95/p99，评测时质量和成本一起看，证明省钱没掉质量。"

### P6 · 框架对齐（证明业界标准，不重复造轮子）
**做什么**：同一 golden set 上，额外跑一条 **Ragas**（faithfulness/answer_relevancy/context_precision）或 **DeepEval**，和自研指标并列进报告做对照。你已有 Day26 的 DeepEval，扩成常规通道即可。
**面试话术**："核心指标我自研可控，同时接了 Ragas/DeepEval 做对照，既能讲清原理又对得上业界标准。"

### CI 门禁强化
- 双门禁：质量（faithfulness/拒答率/hit@k）**和** 成本（单次均价上限）都要过。
- 阈值按维度写进 `ci_gate.py`，进 git 可回溯。
- `.github/workflows/eval-gate.yml` 在 PR 上跑 `pytest + ci_gate + 统计门禁`，红了拦 merge。
- 保留你最强的演示："故意删掉 prompt 拒答约束 → push PR → CI 评测红 → merge 被挡"。

---

## 六、文件落点总表（映射现有代码，不推倒重来）

| 改造项 | 新增/改写 | 复用现有 |
|--------|-----------|----------|
| 分层指标 | 改写 `capstone/evaluation.py` | `day19`(judge)、`day26`(DeepEval) |
| judge 一致性 | 新增 `capstone/eval/judge.py` | `day24` 迁移 |
| 统计层 | 新增 `capstone/eval/stats.py` | 接 `run_eval_platform.py` |
| 真实轨迹 | 改写 `evals/agent_trajectory_eval.py` | `capstone/agent.py` |
| 数据集治理 | `data/golden_set.json`+`eval/dataset.py` | 合并现有 3 份 eval_set |
| 成本/延迟 | 改 `run_eval_platform.py` | `capstone/monitoring.py` |
| 框架对齐 | `eval/framework_ragas.py` | `day26` DeepEval |
| CI 双门禁 | 改 `capstone/ci_gate.py` + workflow | 现有阈值结构 |
| 看板 | 扩 `evals/dashboard.py` | 现有 `reports/*.csv` |

> 建议把散落的评测代码收敛到 `capstone/eval/` 一个包，`dayNN` 文件退化为"这天做哪块"的驱动说明——符合你"只保留高效、以转行为第一性"的原则。

---

## 七、简历写法：改造前 vs 改造后

**改造前（能看出是学习项目）：**
> 搭建 RAG 知识库，使用关键词匹配做自动化评测，配置 CI 检查。

**改造后（可直接投递，每条可量化、可追问）：**

- 设计并实现企业知识库 Agent 的**三层自动化评测平台**（检索/生成/安全），生成层用 **LLM-as-judge** 打忠实度与正确性，检索层量化 Context Recall/Hit@k，将失败精确定位到检索或生成层。
- 针对 LLM 裁判不可靠问题，用**人工标注做一致性校验（Cohen's kappa=0.x）**并做位置偏置控制，一致性低于阈值即不采信该指标——保证评测结论可信。
- 针对 LLM 输出随机性，评测采用**多次采样 + 通过率置信区间（bootstrap）**，回归对比用配对检验区分"真实提升"与"噪声波动"。
- 捕获 **LangGraph Agent 真实执行轨迹**做评测，覆盖工具调用正确性、高危工具拦截、任务完成率与步数异常。
- 将评测固化为 **CI 质量+成本双门禁**（GitHub Actions），质量或成本跌破阈值即拦截 PR 合并；沉淀失败用例库驱动改进闭环。
- 自研核心指标之外接入 **Ragas/DeepEval** 做对照，兼顾可解释性与业界标准对齐。

**一句话电梯稿**：
> "我做的不是一个能聊天的 RAG demo，是一套带 CI 质量门禁的评测平台——它能量化幻觉率、校验 LLM 裁判本身可信不可信、用置信区间判断模型改动是真变好还是噪声。这是我测试工程背景在 AI 岗最直接的迁移。"

---

## 八、优先级建议与工作量

按"简历收益 / 投入"排序，2-3 周节奏：

1. **P0 分层指标**（2-3天）— 收益最高，主指标从字符串升级到语义+检索。
2. **P1 judge 一致性**（1-2天）— 尖刀，多数人没有。
3. **P3 真实轨迹**（1-2天）— 干掉最明显的扣分项。
4. **P2 统计显著性**（1-2天）— 测试背景杀手锏。
5. **P4 数据集治理 + 对抗样本**（1天）— 安全层的弹药。
6. **P5 成本真实化 + P6 框架对齐 + CI 双门禁**（2-3天）— 收尾打磨。

**验收标准**：陌生人 clone 仓库，一条命令跑出带**置信区间的分层指标报告 + judge 一致性 + 真实轨迹评测**，看板能看回归曲线，故意改坏 prompt 能让 CI 变红。做到这个，评测平台就从"拿不上台面"变成简历第一主角。

---

*下一步：确认这个方案后，我可以按 P0→P1→P3 的顺序开始动手改代码（先从重写 `capstone/evaluation.py` 的分层指标开始）。*
